#!/usr/bin/make -f

lib := \$${prefix}/lib/$(DEB_HOST_MULTIARCH)

%:
	dh $@

override_dh_auto_configure:
	dh_auto_configure -- \
		--libdir=$(lib) \
		--dynlibdir=$(lib) \
		--with-dynlib=$(lib) \
		--sysdepdir=$(lib)/skalibs/sysdeps \
		--enable-shared \
		--disable-allstatic \
		--datadir=/etc

